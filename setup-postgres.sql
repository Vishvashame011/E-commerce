-- Create database for ecommerce application
CREATE DATABASE ecommerce;

-- Grant all privileges to postgres user (already has them, but just to be sure)
GRANT ALL PRIVILEGES ON DATABASE ecommerce TO postgres;